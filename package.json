{
  "scripts": {
    "start": "bun src/index.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "check": "bun run typecheck && bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "ffmpeg-static": "latest",
    "sharp": "latest",
    "typescript": "latest"
  }
}
