# Reveal and generation are separate

The user established that a camera-driven Generation Horizon can request background generation beyond the Viewport, while visibility and interaction may be controlled later by explicit triggers or natural Blocker Dependencies. Generated content may form a jagged shape and extend offscreen when required by dependencies; a Generation Request should fill only missing content rather than regenerate an enclosing rectangle.
