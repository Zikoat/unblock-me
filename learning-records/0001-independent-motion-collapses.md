# Independent motion does not multiply decision states

The user established that exact positions must remain available for geometry, while the search should collapse all translations that do not change consequential interactions. In particular, \(n\) independent parallel blocks with \(k\) harmless translations each should contribute one decision state rather than \(k^n\) exact states. This makes interaction boundaries, rather than raw movement distance, the next teaching and validation focus.
