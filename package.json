{
  "scripts": {
    "start": "bun src/index.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "check": "bun run typecheck && bun test",
    "build:web": "bun scripts/build-web.ts",
    "web": "bun run build:web && bun src/web/server.ts",
    "test:web": "node scripts/verify-web.mjs",
    "report:web": "bun scripts/generate-web-report.ts",
    "report:world-modes": "bun scripts/generate-world-modes-report.ts",
    "report:zoom": "bun scripts/generate-zoom-report.ts",
    "report:persistence": "bun scripts/generate-persistence-report.ts",
    "report:feedback": "bun scripts/generate-feedback-report.ts",
    "report:generation-settings": "bun scripts/generate-generation-settings-report.ts",
    "report:evidence-export": "bun scripts/generate-evidence-export-report.ts",
    "report:decision-state": "bun scripts/generate-decision-state-report.ts",
    "report:control-style": "bun scripts/generate-control-style-report.ts",
    "prototype:closure": "bun src/prototypes/dependency-closure/cli.ts",
    "prototype:regions": "bun src/prototypes/multi-region/cli.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "ffmpeg-static": "latest",
    "playwright": "latest",
    "sharp": "latest",
    "typescript": "latest"
  }
}
